// src/types/index.ts

// This file can be used to define shared TypeScript types and interfaces.

// Example (can be removed if not needed initially):
// export interface UserProfile {
//   id: string;
//   name: string;
//   email: string;
// }

// Add shared types here as the application grows.
